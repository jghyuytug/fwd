#ifndef INTER_DISPATCHER_H
#define INTER_DISPATCHER_H

// InterDispatcher - packet dispatcher interface (C structure stub)
typedef struct {
    // Stub implementation for compilation
} InterDispatcher;

#endif
